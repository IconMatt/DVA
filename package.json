{
  "name": "dva-wellbeing",
  "version": "0.1.0",
  "private": true,
  "description": "Front-end build (Tailwind v4 + BEM) for the Veteran and Family Wellbeing Agency site. Intended for later Drupal/GovCMS handoff via Single Directory Components.",
  "scripts": {
    "dev": "tailwindcss -i ./src/main.css -o ./css/main.css --watch",
    "build": "tailwindcss -i ./src/main.css -o ./css/main.css --minify",
    "verify": "npm run build"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.0.0"
  }
}
